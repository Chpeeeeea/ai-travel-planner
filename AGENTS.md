# AI Travel Planner 项目规则

本文件约束本仓库内的研发、数据更新和发布。每次开始工作时，先阅读本文件、`README.md` 与 `CHANGELOG.md`。

## 产品原则

- 这是面向旅行者的产品，不在用户界面展示简历话术、实现复盘、内部数据模型或开发过程说明。
- 卡片负责解释地点、推荐原因和现场看点；地图负责空间核验、真实道路和路线编辑。
- 未完成高德 POI 消歧的地点必须显示为待确认，不得伪装为已核验地点，也不得参与真实道路规划。
- 高德坐标统一使用 GCJ-02；密钥、`securityJsCode`、部署令牌和本地日志不得提交到 Git。

## 阶段更新与发布

以下任一情况都视为一个“阶段更新”：新增用户可见功能、完成一批 POI 核验、修改行程规划逻辑、修复影响使用的问题、调整部署或安全配置。

每个阶段更新必须完成：

1. 在 `CHANGELOG.md` 顶部记录日期、功能变化、数据核验结果和已知限制。
2. 同步更新受影响的产品文案、数据统计、README 和测试；不得只改界面而遗漏数据状态。
3. 运行 `npm.cmd run lint` 与 `npm.cmd test`。失败时不得发布，除非在变更日志和交付说明中明确记录阻塞原因。
4. 检查 `git diff --check`、`git status -sb` 和暂存内容，确认没有密钥、日志或无关文件。
5. 创建聚焦的 Git 提交并推送到 GitHub。默认通过功能分支和 Draft PR 发布；用户明确要求直接更新默认分支时才可直接推送。
6. 若阶段更新影响公开 Demo，同步部署 Sites，并在交付说明中给出 GitHub 提交与线上地址。

如果 GitHub 登录、远程仓库或部署服务不可用，必须保留本地提交并明确报告阻塞，下一次恢复工作时优先完成上传。

## Changelog 规范

- 使用 `YYYY-MM-DD` 日期和语义化版本标题。
- 按需使用 `新增`、`调整`、`修复`、`数据核验`、`部署`、`已知限制` 分类。
- 描述用户能感知的结果，不堆砌代码文件名或内部实现术语。
- POI 核验条目必须包含地点名、数据提供方和仍需确认的风险。

## 完成标准

- 产品页面中文可读，不出现乱码或开发者占位文案。
- 地图道路、遥感图层、行程点和候选点可见且可切换或联动。
- 每个已排程地点包含简介、推荐原因和必要的出行提示。
- 已核验 POI 具有高德 ID、地址、GCJ-02 坐标和核验时间。
- GitHub、公开 Demo、README、`trip.json` 与 `CHANGELOG.md` 对当前阶段的描述一致。
