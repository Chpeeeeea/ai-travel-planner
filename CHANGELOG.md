# Changelog

本文件记录 AI Travel Planner 的阶段性产品更新。每个阶段完成后都必须先更新本文件，再提交并上传 GitHub。

## [0.9.0] - 2026-08-01

### 新增

- 新增登录后的旅行研究工作台 `/studio`：可创建 PlanningRun、查看个人任务列表、恢复选中的运行，并按 5 秒间隔刷新阶段进度。
- 新增面向旅行者的 `/api/trips`：只允许 ChatGPT 登录用户创建和读取自己的任务，返回研究线、候选、核验、排程、路线和运行事件统计。
- PlanningRun 增加 `owner_user_id` 与查询索引；现有受信执行器接口继续使用服务器令牌，浏览器不会获得该令牌或高德 Web Service Key。
- 新增登录用户专用的统一行程数据出口；服务端先检查任务归属，再代理受保护的 `trip.json` 组装接口。
- 首页 Brief 现在可携带目的地、天数、兴趣、特别想吃和必去地点进入工作台；青田仍只作为独立案例展示。

### 产品状态

- 工作台会显示完整七阶段状态机、各主题研究证据数、供应商实际调用数与“下一步声明”。
- 新任务目前会诚实停在 `brief` 阶段并显示“等待 Research Worker”；不会为了演示伪造研究候选、坐标或路线。

### 安全

- 旅行者写入要求同源请求和 ChatGPT 登录身份，任务查询同时校验 `owner_user_id`。
- 公开落地页与青田案例继续匿名可见；只有创建、历史记录和动态结果属于登录区域。

### 已知限制

- 多来源 Research Worker 尚未接入部署环境；底层证据写入、候选编译、POI 核验、排程和道路接口已经就绪，但新任务还不能自动越过研究阶段。
- 已发布动态任务当前先提供统一 JSON 数据；复用现有 Planner 的用户友好动态卡片地图页将在 Research Worker 接通后完成。

## [0.8.0] - 2026-08-01

### 新增

- shortlist 现在可按每批最多 5 个地点调用高德 POI 2.0 核验，保存真实 POI ID、地址、GCJ-02 坐标、置信度和核验时间，并累计实际调用数。
- 同名结果只有在置信度不低于 0.80 且领先第二名至少 0.15 时自动确认；其余候选进入人工消歧，可明确确认一个匹配或拒绝该地点。
- 新增每日编排服务：等全部 shortlist 完成核验后，只从已核验地点中选择每天 4–6 个，并按研究得分、必去约束和地理聚类组织行程。
- 新增相邻道路服务：仅为同一天相邻地点建立 RouteSegment，每批最多计算 5 段；成功结果保存真实距离、耗时和 geometry，失败时不虚构耗时。
- 新增统一旅行数据输出，可由一次 PlanningRun 生成地点卡片、每日安排、相邻路线、研究区视口和质量状态。

### 交互

- 点击左侧行程卡片或候选地点卡片会切换到地图并聚焦相应点位；移动端会先打开地图视图，再等待容器可见后执行跳转。
- “查看简介”、上移、下移和移除继续保持独立动作，不会误触发地图跳转。

### 数据核验

- 使用用户配置的高德 Web Service Key 实测 POI 2.0：`太鹤公园` 返回高德 ID `B024200C41`、青田县地址与 GCJ-02 坐标。
- 使用路径规划 2.0 实测太鹤公园至青田石雕博物馆步行路线：1,768 米、1,414 秒、13 个道路步骤；实时结果可能随路网和算法变化。

### 调用策略

- POI 与路线调用按小批次执行；配额、频率、网络或密钥错误会停止当前批次并保留未处理项，重试不会重新请求已完成结果。
- 编排服务本身调用高德 0 次；路线总数严格等于各日 `N−1` 之和，不为候选池或未入选地点计算路线。

### 已知限制

- 服务 API 当前仍由受信执行器令牌保护；旅行者可查看进度、处理消歧并打开动态行程的产品页面属于下一阶段。
- 当前自动路线模式支持步行、驾车和骑行；公交 Route 2.0 需要起终点 citycode，将在旅行者交通偏好界面一并接入。

## [0.7.0] - 2026-08-01

### 新增

- PlanningRun 现在可以批量接收官方文旅、小红书、OSM 和多主题研究证据；重复提交同一来源时幂等更新，不重复堆积。
- 新增服务端候选编译：跨主题合并别名，综合来源权威度、平台多样性、兴趣匹配、证据数量和风险进行评分。
- 新增候选审阅结果，集中展示推荐理由、现场看点、停留时间、风险、来源及是否进入最终核验清单。
- Brief 新增“特别想吃”和“必去地点”自定义需求；下载、持久化、候选排序与审阅使用同一份约束。
- 有研究证据支持的必去地点优先进入 shortlist；菜品与候选介绍匹配时提高排序，但两类需求都不会绕过真实 POI 核验。

### 调用策略

- 研究证据与候选编译阶段不读取高德密钥，POI 与路线调用增量均为 0。
- 每次运行只持久化排名后的 20–40 个名称级候选；不足 20 个时保留警告，不虚构地点补齐数量。
- 新建 Brief 的候选范围固定为 20–40，每日地点范围固定为 4–6，避免前端输入绕过产品边界。

### 已知限制

- 当前研究证据由受信研究执行器批量写入；面向旅行者的异步进度页和账号权限仍未开放。
- 高德 shortlist 核验、每日自动编排和相邻道路服务仍是下一阶段。

## [0.6.1] - 2026-08-01

### 修复

- 修复地图容器由隐藏转为可见时调用不存在的尺寸刷新方法，导致研究区视口没有真正应用的问题。
- 地图资源加载完成且容器可见后，立即按当前旅行的 `map_view` 聚焦研究区，避免过渡动画被道路绘制打断；青田案例首次进入地图会覆盖青田县研究范围。

### 交互

- 正式行程点与候选点双击后平滑放大到该地点，并保持地图点位与卡片选中状态一致。
- 点位放大和返回研究区都会显示地点或目的地反馈，研究区提示不再写死案例城市。

## [0.6.0] - 2026-08-01

### 新增

- 新增持久化 PlanningRun：保存旅行 Brief、研究证据、候选池、高德匹配、每日安排、相邻路线和阶段事件。
- 新增受服务端令牌保护的任务 API，可创建、查询并按顺序推进 `brief → researching → shortlisted → verifying → scheduled → routing → published`。
- 每次阶段推进累计记录高德 POI 与路线调用数，为配额控制、失败重试和成本复盘提供数据。

### 数据层

- 接入 D1 并生成首个 9 表迁移；为任务状态、候选名称、消歧结果、日程顺序和路线状态添加实际查询索引。
- 研究候选与高德实体继续分表保存，避免未核验名称污染最终地点。

### 安全

- PlanningRun API 默认不可写；只有配置服务端 `PLANNING_RUN_WRITE_TOKEN` 后才接受请求，公开网页不会获得该密钥。

### 已知限制

- 当前任务 API 面向后续研究执行器；终端用户登录、个人任务列表与浏览器直接创建持久化任务尚未开放。

## [0.5.0] - 2026-08-01

### 新增

- 新增平台产品首页：用户可填写目的地、天数和兴趣，生成标准旅行 Brief，并看到候选核验与真实路线的预计调用量。
- 新增可执行平台管线：将多源研究证据编译为名称级候选池，生成高德核验清单，再把已核验地点编排为每天 4–6 个地点和相邻路线清单。
- 仓库纳入可复用 Skill 源码和管线测试，使平台流程与 Codex 旅行规划工作流使用同一套阶段边界。

### 调整

- 青田三日从产品首页迁移到 `/cases/qingtian`，明确作为第一条真实目的地案例展示。
- 仓库名称与说明从“青田 Demo”调整为 AI Travel Planner 平台，青田数据移动到独立案例目录。
- Next.js 升级到当前稳定补丁版本，并覆盖存在安全公告的生产依赖；生产依赖审计无已知漏洞。

### 调用策略

- 官方文旅、小红书、OSM 与多主题研究阶段不调用高德。
- 高德只核验去重评分后的 20–40 个候选；路线只计算每日相邻行程点，不为候选池批量计算道路。

### 已知限制

- 当前公开站可生成和下载研究 Brief；自动抓取、异步研究任务、账号与持久化运行记录仍属于后续平台阶段。

## [0.4.0] - 2026-08-01

### 新增

- 正式行程点与候选点支持双击放大，放大后保持卡片和地图选中状态同步。
- 地图图层控制增加“研究区”按钮，可随时返回青田县旅行全域。

### 修复

- 修复移动端地图默认隐藏时提前计算视口，导致首次切换到地图没有聚焦青田县的问题。
- 地图从隐藏视图恢复后会先刷新容器尺寸，再应用研究区边界；只有可见且尺寸有效时才记录初始聚焦完成。

### 规则

- 响应式标签页中的地图必须在隐藏转可见后执行尺寸刷新并恢复目的地视口；点位交互需提供双击聚焦和返回研究区入口。

## [0.3.0] - 2026-08-01

### 新增

- 平板和手机增加“行程卡片 / 路线地图 / 候选地点”三视图切换，移动端可以访问与桌面端相同的核心功能。
- 行程数据增加目的地地图视口；首次打开地图自动聚焦青田县研究区。

### 调整

- 移动端采用同一套组件和数据的响应式布局，不维护第二套产品逻辑。
- 地图首次加载保持研究区全貌；切换日期或编辑路线后再聚焦当天线路，远端候选点不再把路线缩得过小。

### 规则

- 项目验收新增 360px 手机、768px 平板和桌面端适配要求。
- 核心功能不得只在桌面出现，地图默认视口必须由旅行目的地数据提供。

## [0.2.0] - 2026-08-01

### 新增

- 地图支持“道路 / 遥感”切换；遥感模式叠加高德路网和地名。
- 候选地点显示在地图上，并可智能插入当天行程。
- 行程地点支持查看简介、推荐原因、现场看点、上移、下移和移除。
- 调整站点后按当前顺序重新计算高德真实道路。

### 调整

- 产品介绍改为面向旅行者的功能说明，移除简历话术和开发过程叙述。
- 行程卡片与地图采用双向选中状态，候选点和正式行程使用不同标记。

### 修复

- 修复行程摘要中文乱码，并为 Markdown 下载增加 UTF-8 BOM。
- 修复地图只能显示路线示意、无法呈现真实道路的问题。

### 数据核验

- 通过高德核验“东源镇东源村章乃器生平事迹陈列馆”，补充 POI ID、地址和 GCJ-02 坐标。
- 通过高德核验“青田稻鱼共生系统博物馆”，补充 POI ID、地址和 GCJ-02 坐标。
- “青田华侨历史陈列馆”仍待消歧：高德当前返回名称不同的“阜山华侨历史陈列馆”，暂不自动合并。

### 部署

- Sites Demo 已公开，并通过服务端代理保护高德 `securityJsCode`。

## [0.1.0] - 2026-07-31

### 新增

- 建立青田三日旅行 Demo，覆盖历史、文化、风景和美食主题。
- 提供三日卡片行程、候选地点、地图视图、行程摘要和产品介绍页。
- 接入高德 POI、GCJ-02 坐标和相邻路段数据。
