# Changelog

本文件记录 AI Travel Planner 的阶段性产品更新。每个阶段完成后都必须先更新本文件，再提交并上传 GitHub。

## [0.13.1] - 2026-08-02

### 行程路线增量重算

- 插入、移除或重排行程地点时保留已有 Assignment ID，不再删除当天全部 Assignment 和真实道路。
- 新增有向相邻点对差分：仅删除已断开的 RouteSegment，保留仍然相邻且方向未变的道路，并只为新增点对重新排队。
- 行程编辑接口返回失效、保留和待计算路段数量，运行事件同步记录本次实际影响范围，便于成本复盘。
- 新增路线差分单元测试和防止整日路线删除的源码回归断言；README 与仓库/本机 Skill 同步更新。

## [0.13.0] - 2026-08-02

### 多用户用量边界

- 新增逐用户任务与月度高德配额：默认同时 3 个进行中任务、每月 10 次新任务、200 次 POI 核验和 200 次相邻路线调用；均可通过生产环境变量调整。
- 新增供应商用量流水，按用户、任务、调用类型和时间记录每个真实小批次；工作台显示本月 POI/路线用量、剩余可建任务和重置日期。
- POI/路线接口在服务器侧按剩余额度缩小批次，额度用尽时返回结构化状态；Worker 将任务释放为 `awaiting_quota`，下月自动恢复且不增加失败尝试次数。
- 一个共享 Research Worker 继续服务多用户队列；配额属于网站与数据库可信边界，不拆分为新 Skill，研究 Agent 仍不获得高德密钥。

### 只读旅行分享

- 已发布动态行程新增“分享行程”：可创建 7/30/90 天或长期有效的只读链接，每个行程最多 5 条有效链接。
- 分享 Token 只在创建时返回，D1 仅保存 SHA-256 摘要；所有者可以随时撤销，过期、撤销或行程不再发布时公开页面立即 404。
- 公开分享页只渲染卡片与地图，不提供编辑、私人导出、任务历史或用户身份，并设置 `noindex` 与 `no-referrer`。

### 数据与验证

- 新增 `provider_usage_events`、`trip_share_links` 两张 D1 表及查询索引；补齐环境变量示例、README、Skill 平台契约和 Worker 配额停等测试。
- 版本升级为 0.13.0；迁移、类型检查、lint、生产构建与完整测试在发布前执行。

## [0.12.2] - 2026-08-02

### 首页信息架构

- 参考成熟工具平台“首屏即产品、能力带解释依赖、模块化卡片讲完整流程”的结构原则，继续保留 AI Travel Planner 的暖绿、米白和橙色，不复制参考站的严肃蓝灰配色。
- 顶部导航改为产品能力、工作流程、案例和开始规划四个明确入口；移动端保留主要操作，减少无效导航占位。
- 首屏继续直接承载可操作旅行 Brief，并新增 38 个研究主题指标；整体缩短区块间距和标题尺度，提高同屏信息量。
- 能力带从四项来源扩展为六项完整依赖链：官方文旅、小红书、OSM、38 主题 Agent、高德 POI 与真实道路。
- 工作流程、供应商成本控制和案例模块同步压缩垂直留白，桌面、平板、移动端分别使用 6/3/2 列能力布局。

### 验证

- 首页继续服务端渲染真实产品文案与操作入口；补充六项能力依赖和导航锚点回归检查。

## [0.12.1] - 2026-08-02

### 产品体验

- 旅行主题目录从 16 项扩展为 38 项，覆盖人文与城市、自然与户外、吃喝与生活、兴趣与娱乐、旅行方式五类；历史、文化、风景、美食继续作为默认推荐，但不再构成产品边界。
- 首页与旅行工作台新增分类切换式多选器，桌面端和移动端均可浏览完整目录；每个主题显示独立研究范围，达到 8 条研究线后禁用未选项，避免无提示地丢失用户选择。
- 旅行工作台改为紧凑的应用布局：压缩展示型首屏、固定轻量顶栏、收窄任务侧栏并扩大主表单区域；大屏不再把表单挤在左侧，移动端按单列重排。
- 已选主题独立显示为可移除标签，主题目录默认从分类页签进入，避免 38 个按钮一次铺满工作台首屏。
- 新增工业遗产、文学名人、骑行、露营观星、海滨海岛、亲水、冰雪、动物生态、菜场夜市、咖啡甜品、茶酒、手作、演出、节庆、体育赛事、乐园、科技、自驾、火车、无障碍和宠物同行等细分检索方向。

### 工作流与文档

- Research Worker 继续直接读取统一主题目录中的 `scope` 生成专题 Prompt；新增主题自动进入持久化 ResearchLaneJob，不在 Worker 内维护第二份固定清单。
- README、仓库 Skill 和研究编排参考同步为 38 项动态目录，并保留最多 8 条研究线、最多 4 条并发的速度与质量边界。

### 验证

- 新增主题总量、分类完整性、专业兴趣别名映射和首页可见性回归测试。

## [0.12.0] - 2026-08-01

### 新增

- 新增登录用户专属的同名/跨城 POI 消歧页：逐项展示高德候选名称、城市、区县、地址、类型、置信度和地图查看入口，可确认正确实体或排除整个错误候选。
- 工作台在出现 `needs_confirmation` 时提供明确操作入口；最后一项处理完成后，PlanningRun 自动从等待确认恢复为 `verifying/queued`，Research Worker 在线时继续排程与真实道路。
- 新增身份隔离的旅行者消歧 API；读取与写入都校验 `owner_user_id`，写入额外要求同源请求和候选仍处于待确认状态。

### 产品与安全

- 人工确认只复用已有高德匹配，供应商调用增量固定为 0；未选择带坐标的真实实体不能确认，排除项不会进入日程或路线。
- 工作台的研究阶段文案改为“用户所选主题线”，不再把动态研究误写为固定四主题。
- Skill 平台规则同步要求消歧页面展示可核对信息、保持用户归属边界，并在全部处理完后恢复持久化队列。

### 验证

- 新增消歧页登录保护、用户归属、同源写入、状态约束、零供应商调用、队列恢复和产品文案测试。
- 生产构建、lint、TypeScript 检查、38 项 Node 测试与 4 项 Python 管线测试通过。

## [0.11.1] - 2026-08-01

### 部署准备

- 新增 2 核 2G 阿里云 Worker 环境模板与 systemd 服务；默认研究并发降为 1，设置进程重启、内存高水位和 1.8 GB 硬上限，避免低配主机同时拉起多个 Codex 进程。
- 新增 `worker:research -- --check`：只检查 Codex CLI、平台令牌和受保护 API，不领取或修改旅行任务，可作为 systemd 启动前检查。
- README 与 Worker 手册补齐 ECS、Node 22、Codex 登录、仓库 Skill、`vibe-web-research`、令牌文件权限、日志和安全组清单；无域名的 ECS 也可作为主动轮询 Worker。
- 明确 headless ECS 默认只保证官方网页、开放 Web 与 OSM 覆盖；小红书等登录平台需要合法登录态或 Browser Bridge，不允许绕过登录和验证码。

### 验证

- 新增低内存部署配置、启动前检查、密钥隔离和 systemd 不内嵌令牌的回归测试。
- lint、TypeScript 检查与 Research Worker 单元测试通过；完整产品构建与管线测试在提交前再次执行。

## [0.11.0] - 2026-08-01

### 新增

- 新增登录用户专属的动态行程页 `/trip?run_id=...`：服务端同时校验 ChatGPT 身份与 `owner_user_id`，再用统一 Trip 数据渲染日程卡片、候选池、高德道路/遥感地图和导航入口。
- 新增目的地无关的 Trip 组装运行时与服务层；青田继续作为参考案例，沈阳等任意目的地不再依赖写死的城市、天数、主题或统计数字。
- 新增旅行者行程编辑接口：候选点插入、地点移除、上下重排与恢复默认都会持久化到 D1；旧 RouteSegment 同批失效，任务回到路线队列等待真实相邻道路重算。
- 新增身份隔离的 Markdown 与 GeoJSON 导出；Markdown 使用 UTF-8 BOM，GeoJSON 只导出已核验点位和确实保存的路线 geometry，不把端点占位伪装成真实道路。

### 产品体验

- Planner 现在从实际 POI 自动生成主题筛选，可适应用户从 16 项旅行主题目录和自定义兴趣中选择的研究结果，不再限定历史、文化、风景、美食四类。
- 工作台发布后的主入口改为“打开卡片地图”，原始 JSON 降为技术入口；地图页显示持久化保存、道路重算排队和失败状态。
- 动态结果沿用移动端“行程卡片 / 路线地图 / 候选地点”三视图，以及卡片、候选与 Marker 的双向定位。

### 验证

- 新增目的地无关 Trip、可读 Markdown、真实 GeoJSON、动态页身份归属、写入同源保护、已核验地点约束、路线失效和串行持久化测试。
- `lint`、TypeScript 类型检查、生产构建、36 项 Node 测试与 4 项 Python 管线测试全部通过。

### 尚待部署

- 公共 Sites 页面可以运行动态产品与 D1，但 Codex Research Worker 仍需在独立常驻主机启动；未部署时新任务会诚实停留在 `brief`。
- 同名或跨城 POI 的人工消歧页面仍需补齐；当前底层接口和 `needs_confirmation` 状态已经就绪。

## [0.10.0] - 2026-08-01

### 新增

- PlanningRun 增加短期租约、Worker 版本、执行尝试数和过期恢复字段；多个 Worker 同时轮询时，只有原子领取成功的实例可以获得租约 Token。
- 新增 16 项旅行主题目录：历史、文化、风景、美食之外，覆盖建筑、博物馆、艺术、在地生活、亲子、夜游、购物、户外、摄影、康养、信仰与影视动漫；用户最多选择 8 项，目录外兴趣进入自定义研究线。
- 新增动态持久化研究任务，分别记录等待、检索中、完成、待重试、主题名、尝试次数、证据数、Markdown 研究文档和错误。
- 新增可执行的 Codex Research Worker：按所选主题有界并发调用 `ai-travel-planner` 与 `vibe-web-research`，使用 JSON Schema 约束输出，随后由主进程串行推进候选编译、高德核验、每日排程和相邻道路。
- 工作台现在展示 Worker 是否在线、租约尝试次数以及所有已选主题研究线的真实执行状态。

### 安全与恢复

- 租约 Token 只以 SHA-256 摘要保存；心跳、研究线状态和释放操作都必须同时通过服务器 Bearer Token 与有效租约校验。
- Codex 子进程采用只读沙箱，并在启动前移除平台写入令牌和全部高德密钥；网页内容只作为研究材料，不能改变任务指令。
- Worker 崩溃后租约自动过期；新实例跳过已完成研究线和后续阶段，避免重复研究及重复消费高德调用。
- 单研究线最多尝试 3 次，单 PlanningRun 最多被领取 5 次；超过上限或确定性数据不足时进入明确失败/人工确认状态，不无限重试。
- 使用 Sites 生产环境一致的 npm 10 重新生成 lockfile，保证 Linux 构建机能够用 `npm ci` 完成可复现安装。

### 文档与 Skill

- 仓库 Skill 移至 Codex 官方支持的 `.agents/skills/ai-travel-planner` 自动发现位置，仍以仓库版本为事实源。
- 新增 Research Worker 运行、环境变量、常驻模式、凭据边界与低配置服务器建议；README 同步更新真实运行状态和接口。
- 已将此前 Draft PR #7 合并到 GitHub `main`，仓库首页可以直接看到完整产品 Flow、运行依赖和分 Agent 编排说明。

### 已知限制

- Worker 代码与平台接口已经实现，但公共 Sites 环境仍需要一台独立主机或容器实际常驻运行 Codex；未启动时新任务会停在 `brief`。
- 动态任务发布后仍先提供统一行程 JSON；直接复用 Planner 的卡片地图结果页和面向旅行者的 POI 消歧页仍待完成。

## [0.9.0] - 2026-08-01

### 新增

- 新增登录后的旅行研究工作台 `/studio`：可创建 PlanningRun、查看个人任务列表、恢复选中的运行，并按 5 秒间隔刷新阶段进度。
- 新增面向旅行者的 `/api/trips`：只允许 ChatGPT 登录用户创建和读取自己的任务，返回研究线、候选、核验、排程、路线和运行事件统计。
- PlanningRun 增加 `owner_user_id` 与查询索引；现有受信执行器接口继续使用服务器令牌，浏览器不会获得该令牌或高德 Web Service Key。
- 新增登录用户专用的统一行程数据出口；服务端先检查任务归属，再代理受保护的 `trip.json` 组装接口。
- 首页 Brief 现在可携带目的地、天数、兴趣、特别想吃和必去地点进入工作台；青田仍只作为独立案例展示。

### 产品状态

- 工作台会显示完整七阶段状态机、各主题研究证据数、供应商实际调用数与“下一步声明”。
- 新任务目前会诚实停在 `brief` 阶段并显示“等待 Research Worker”；不会为了演示伪造研究候选、坐标或路线。

### 文档

- 重构仓库 README：以通用产品 Flow 为主线，分开旅行者、Research Worker、地图和数据层，增加能力状态、服务边界、环境变量、仓库结构与下一阶段说明；青田移至参考案例章节。
- README 新增 Skill 运行位置、历史/文化/风景/美食分 Agent 契约、`vibe-web-research` 与 `content-analysis` 依赖，以及仓库 Skill 与本机安装副本的同步规则。
- `.env.example` 补齐服务器侧 `AMAP_WEBSERVICE_KEY`，明确与浏览器 JSAPI Key 分开配置。

### 安全

- 旅行者写入要求同源请求和 ChatGPT 登录身份，任务查询同时校验 `owner_user_id`。
- 公开落地页与青田案例继续匿名可见；只有创建、历史记录和动态结果属于登录区域。

### 已知限制

- 多来源 Research Worker 尚未接入部署环境；底层证据写入、候选编译、POI 核验、排程和道路接口已经就绪，但新任务还不能自动越过研究阶段。
- 已发布动态任务当前先提供统一 JSON 数据；复用现有 Planner 的用户友好动态卡片地图页将在 Research Worker 接通后完成。

## [0.8.0] - 2026-08-01

### 新增

- shortlist 现在可按每批最多 5 个地点调用高德 POI 2.0 核验，保存真实 POI ID、地址、GCJ-02 坐标、置信度和核验时间，并累计实际调用数。
- 同名结果只有在置信度不低于 0.80 且领先第二名至少 0.15 时自动确认；其余候选进入人工消歧，可明确确认一个匹配或拒绝该地点。
- 新增每日编排服务：等全部 shortlist 完成核验后，只从已核验地点中选择每天 4–6 个，并按研究得分、必去约束和地理聚类组织行程。
- 新增相邻道路服务：仅为同一天相邻地点建立 RouteSegment，每批最多计算 5 段；成功结果保存真实距离、耗时和 geometry，失败时不虚构耗时。
- 新增统一旅行数据输出，可由一次 PlanningRun 生成地点卡片、每日安排、相邻路线、研究区视口和质量状态。

### 交互

- 点击左侧行程卡片或候选地点卡片会切换到地图并聚焦相应点位；移动端会先打开地图视图，再等待容器可见后执行跳转。
- “查看简介”、上移、下移和移除继续保持独立动作，不会误触发地图跳转。

### 数据核验

- 使用用户配置的高德 Web Service Key 实测 POI 2.0：`太鹤公园` 返回高德 ID `B024200C41`、青田县地址与 GCJ-02 坐标。
- 使用路径规划 2.0 实测太鹤公园至青田石雕博物馆步行路线：1,768 米、1,414 秒、13 个道路步骤；实时结果可能随路网和算法变化。

### 调用策略

- POI 与路线调用按小批次执行；配额、频率、网络或密钥错误会停止当前批次并保留未处理项，重试不会重新请求已完成结果。
- 编排服务本身调用高德 0 次；路线总数严格等于各日 `N−1` 之和，不为候选池或未入选地点计算路线。

### 已知限制

- 服务 API 当前仍由受信执行器令牌保护；旅行者可查看进度、处理消歧并打开动态行程的产品页面属于下一阶段。
- 当前自动路线模式支持步行、驾车和骑行；公交 Route 2.0 需要起终点 citycode，将在旅行者交通偏好界面一并接入。

## [0.7.0] - 2026-08-01

### 新增

- PlanningRun 现在可以批量接收官方文旅、小红书、OSM 和多主题研究证据；重复提交同一来源时幂等更新，不重复堆积。
- 新增服务端候选编译：跨主题合并别名，综合来源权威度、平台多样性、兴趣匹配、证据数量和风险进行评分。
- 新增候选审阅结果，集中展示推荐理由、现场看点、停留时间、风险、来源及是否进入最终核验清单。
- Brief 新增“特别想吃”和“必去地点”自定义需求；下载、持久化、候选排序与审阅使用同一份约束。
- 有研究证据支持的必去地点优先进入 shortlist；菜品与候选介绍匹配时提高排序，但两类需求都不会绕过真实 POI 核验。

### 调用策略

- 研究证据与候选编译阶段不读取高德密钥，POI 与路线调用增量均为 0。
- 每次运行只持久化排名后的 20–40 个名称级候选；不足 20 个时保留警告，不虚构地点补齐数量。
- 新建 Brief 的候选范围固定为 20–40，每日地点范围固定为 4–6，避免前端输入绕过产品边界。

### 已知限制

- 当前研究证据由受信研究执行器批量写入；面向旅行者的异步进度页和账号权限仍未开放。
- 高德 shortlist 核验、每日自动编排和相邻道路服务仍是下一阶段。

## [0.6.1] - 2026-08-01

### 修复

- 修复地图容器由隐藏转为可见时调用不存在的尺寸刷新方法，导致研究区视口没有真正应用的问题。
- 地图资源加载完成且容器可见后，立即按当前旅行的 `map_view` 聚焦研究区，避免过渡动画被道路绘制打断；青田案例首次进入地图会覆盖青田县研究范围。

### 交互

- 正式行程点与候选点双击后平滑放大到该地点，并保持地图点位与卡片选中状态一致。
- 点位放大和返回研究区都会显示地点或目的地反馈，研究区提示不再写死案例城市。

## [0.6.0] - 2026-08-01

### 新增

- 新增持久化 PlanningRun：保存旅行 Brief、研究证据、候选池、高德匹配、每日安排、相邻路线和阶段事件。
- 新增受服务端令牌保护的任务 API，可创建、查询并按顺序推进 `brief → researching → shortlisted → verifying → scheduled → routing → published`。
- 每次阶段推进累计记录高德 POI 与路线调用数，为配额控制、失败重试和成本复盘提供数据。

### 数据层

- 接入 D1 并生成首个 9 表迁移；为任务状态、候选名称、消歧结果、日程顺序和路线状态添加实际查询索引。
- 研究候选与高德实体继续分表保存，避免未核验名称污染最终地点。

### 安全

- PlanningRun API 默认不可写；只有配置服务端 `PLANNING_RUN_WRITE_TOKEN` 后才接受请求，公开网页不会获得该密钥。

### 已知限制

- 当前任务 API 面向后续研究执行器；终端用户登录、个人任务列表与浏览器直接创建持久化任务尚未开放。

## [0.5.0] - 2026-08-01

### 新增

- 新增平台产品首页：用户可填写目的地、天数和兴趣，生成标准旅行 Brief，并看到候选核验与真实路线的预计调用量。
- 新增可执行平台管线：将多源研究证据编译为名称级候选池，生成高德核验清单，再把已核验地点编排为每天 4–6 个地点和相邻路线清单。
- 仓库纳入可复用 Skill 源码和管线测试，使平台流程与 Codex 旅行规划工作流使用同一套阶段边界。

### 调整

- 青田三日从产品首页迁移到 `/cases/qingtian`，明确作为第一条真实目的地案例展示。
- 仓库名称与说明从“青田 Demo”调整为 AI Travel Planner 平台，青田数据移动到独立案例目录。
- Next.js 升级到当前稳定补丁版本，并覆盖存在安全公告的生产依赖；生产依赖审计无已知漏洞。

### 调用策略

- 官方文旅、小红书、OSM 与多主题研究阶段不调用高德。
- 高德只核验去重评分后的 20–40 个候选；路线只计算每日相邻行程点，不为候选池批量计算道路。

### 已知限制

- 当前公开站可生成和下载研究 Brief；自动抓取、异步研究任务、账号与持久化运行记录仍属于后续平台阶段。

## [0.4.0] - 2026-08-01

### 新增

- 正式行程点与候选点支持双击放大，放大后保持卡片和地图选中状态同步。
- 地图图层控制增加“研究区”按钮，可随时返回青田县旅行全域。

### 修复

- 修复移动端地图默认隐藏时提前计算视口，导致首次切换到地图没有聚焦青田县的问题。
- 地图从隐藏视图恢复后会先刷新容器尺寸，再应用研究区边界；只有可见且尺寸有效时才记录初始聚焦完成。

### 规则

- 响应式标签页中的地图必须在隐藏转可见后执行尺寸刷新并恢复目的地视口；点位交互需提供双击聚焦和返回研究区入口。

## [0.3.0] - 2026-08-01

### 新增

- 平板和手机增加“行程卡片 / 路线地图 / 候选地点”三视图切换，移动端可以访问与桌面端相同的核心功能。
- 行程数据增加目的地地图视口；首次打开地图自动聚焦青田县研究区。

### 调整

- 移动端采用同一套组件和数据的响应式布局，不维护第二套产品逻辑。
- 地图首次加载保持研究区全貌；切换日期或编辑路线后再聚焦当天线路，远端候选点不再把路线缩得过小。

### 规则

- 项目验收新增 360px 手机、768px 平板和桌面端适配要求。
- 核心功能不得只在桌面出现，地图默认视口必须由旅行目的地数据提供。

## [0.2.0] - 2026-08-01

### 新增

- 地图支持“道路 / 遥感”切换；遥感模式叠加高德路网和地名。
- 候选地点显示在地图上，并可智能插入当天行程。
- 行程地点支持查看简介、推荐原因、现场看点、上移、下移和移除。
- 调整站点后按当前顺序重新计算高德真实道路。

### 调整

- 产品介绍改为面向旅行者的功能说明，移除简历话术和开发过程叙述。
- 行程卡片与地图采用双向选中状态，候选点和正式行程使用不同标记。

### 修复

- 修复行程摘要中文乱码，并为 Markdown 下载增加 UTF-8 BOM。
- 修复地图只能显示路线示意、无法呈现真实道路的问题。

### 数据核验

- 通过高德核验“东源镇东源村章乃器生平事迹陈列馆”，补充 POI ID、地址和 GCJ-02 坐标。
- 通过高德核验“青田稻鱼共生系统博物馆”，补充 POI ID、地址和 GCJ-02 坐标。
- “青田华侨历史陈列馆”仍待消歧：高德当前返回名称不同的“阜山华侨历史陈列馆”，暂不自动合并。

### 部署

- Sites Demo 已公开，并通过服务端代理保护高德 `securityJsCode`。

## [0.1.0] - 2026-07-31

### 新增

- 建立青田三日旅行 Demo，覆盖历史、文化、风景和美食主题。
- 提供三日卡片行程、候选地点、地图视图、行程摘要和产品介绍页。
- 接入高德 POI、GCJ-02 坐标和相邻路段数据。
